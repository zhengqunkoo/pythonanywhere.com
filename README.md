# https://zhengqunkoo.pythonanywhere.com/

# Local Development

1. Ensure you have Docker and Docker Compose installed.
2. Run `docker compose up --build`.
3. Access the application at `http://localhost:5000`.
